<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net7.0</TargetFrameworks>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <PublishAot>false</PublishAot>
  </PropertyGroup>

  <Target Name="CopyLibs Windows x64" AfterTargets="AfterBuild">
    <Copy SourceFiles="libs/volt.dll" DestinationFolder="$(OutDir)" />
  </Target>

  <Target Name="CopyLibs Linux x64" AfterTargets="AfterBuild">
    <Copy SourceFiles="libs/libvolt.so" DestinationFolder="$(OutDir)" />
  </Target>

  <ItemGroup>
    <Content Remove="**/Examples/**" />
    <Compile Remove="**/Examples/**" />
    <EmbeddedResource Remove="**/Examples/**" />
    <None Remove="**/Examples/**" />
  </ItemGroup>

</Project>
